<h1>Editing page</h1>

<% form_for(@page) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
 <p>
    <%= f.label :position, 'Posición' %><br />
	<%= select_tag "page[position]", options_for_select(Page.find(:all, :order => :position).map {|p| [p.position.to_s + " - " + p.name, p.position]}, @page.position) %>
  </p>
<p>
    <%= f.label :Content, "Contenido" %><br />
    <%= f.text_area :content %>
  </p>
    <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>
